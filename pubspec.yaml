name: widget_spinning_wheel
description: Customisable widget for a 'spin the wheel'
version: 0.0.3
homepage: https://github.com/MenuItem207/widget_spinning_wheel

environment:
  sdk: '>=3.0.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
