name: widget_spinning_wheel
description: Customisable widget for a 'spin the wheel'
version: 0.0.4
homepage: https://github.com/MenuItem207/widget_spinning_wheel
  
screenshots:
  - description: 'Screenshot image for the widget'
    path: spin.png

environment:
  sdk: '>=3.0.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
